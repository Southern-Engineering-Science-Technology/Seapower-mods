[------------------------- Radar ---------------------------]

[---Air/Air&Surface---]

[N035_Shmel]
#Users: wp_a-50
Kind=Radar
Type=Search            // Classes can be "DirectedSearch", "Search", "Targeting"
HasDataLink=True       // true means the sensor data will be used for the entire task force via data link, false local use only
Role=AirAndSurface     // Role can be "Surface", "Air", "AirAndSurface"
Mode=Illuminate        // Can be "Illuminate", "RadioCommand"
RotationDirection=1    // -1 means left, 1 means right
RPM=6                 // revolutions per minute
MinRange=0.2           // min range in km
MaxRange=400.0         // max range in km
MinAltitude=0          // min altitude in meters, relevant for air, unused for surface
MaxAltitude=30480      // max altitude in meters, relevant for air, unused for surface
RangeResolution=110.0  // defines the minimum range where two targets behind can be recognized as two; in meters
BeamWidth=3.5          // defines the beam width in degrees for BearingResolution >= 2*Dist*sin(BeamWidth/2)
Gain=45                // gain in dB on the sensor
Frequency=L-Band       // frequency band
PeakPower=960.0       // output peak power in kW

[N035M_Shmel-M]
#Users: wp_a-50u
Kind=Radar
Type=Search            // Classes can be "DirectedSearch", "Search", "Targeting"
HasDataLink=True       // true means the sensor data will be used for the entire task force via data link, false local use only
Role=AirAndSurface     // Role can be "Surface", "Air", "AirAndSurface"
Mode=Illuminate        // Can be "Illuminate", "RadioCommand"
RotationDirection=1    // -1 means left, 1 means right
RPM=8                 // revolutions per minute
MinRange=0.2           // min range in km
MaxRange=500.0         // max range in km
MinAltitude=0          // min altitude in meters, relevant for air, unused for surface
MaxAltitude=35000      // max altitude in meters, relevant for air, unused for surface
RangeResolution=75.0  // defines the minimum range where two targets behind can be recognized as two; in meters
BeamWidth=3.0          // defines the beam width in degrees for BearingResolution >= 2*Dist*sin(BeamWidth/2)
Gain=50                // gain in dB on the sensor
Frequency=L-Band       // frequency band
PeakPower=1200.0       // output peak power in kW

[---------------------------------------- ESM ----------------------------------------]
[ ---------- RWR ---------- ]

[A-50_RWR]
Kind=ESM
Type=RWR
Gain=6                             // Slightly higher gain for larger platform with better sensors
HasDataLink=True                   // Shares detected threats with task force
AngularResolution=20.0             // Better resolution than generic aircraft due to advanced ESM suite
Frequencies=L-Band,S-Band,C-Band,X-Band  // Wider band coverage typical for A-50's ESM system

[A-50U_RWR]
Kind=ESM
Type=RWR
Gain=8                             // Improved signal processing & antennas
HasDataLink=True                   // Still shares threat data with task force
AngularResolution=15.0             // Finer angular resolution with upgraded ESM suite
Frequencies=E-Band,L-Band,S-Band,C-Band,X-Band  // Expanded frequency coverage

[---------------------------------------- ECM ----------------------------------------]

[A-50_DECM]
#Users: wp_a-50
Kind=ECM
Frequencies=L-Band,S-Band,C-Band,X-Band   // Covers common radar bands relevant to threats against A-50
Type=Defensive       // Defensive jammer, protecting itself from radar-guided threats
JamChance=0.15       // Slightly lower chance than B-52D, balanced for AEW&C role

[A-50U_DECM]
#Users: wp_a-50u
Kind=ECM
Frequencies=E-Band,L-Band,S-Band,C-Band,X-Band   // Expanded coverage for modern threats
Type=Defensive
JamChance=0.25       // Improved jamming effectiveness with modern suite

