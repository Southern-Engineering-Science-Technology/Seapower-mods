[Animations]
NumberOfAnimations=1

Animation1=SA-2_Reload

[SA-2_Reload]
NumberOfSequencesToPlay=2
Sequence1=WaitForReload
Sequence1_Model=SpawnedObject  // doesn't matter here, spawned object (RBU) is used, keep the entry so the logfile doesn't contain a warning
Sequence2=SpawnMissile
Sequence2_Model=SpawnedObject  // doesn't matter here, spawned object (RBU) is used, keep the entry so the logfile doesn't contain a warning

[WaitForReload]
NumberOfSteps=1
Time1=0
Time2=699

[SpawnMissile]
UseLauncherPositionOffsets=True  // optional, if true means that it uses the launcher position offsets from the ammunition file in section [Launchers]
Spawn=True
NumberOfSteps=1
Time1=700
PositionZ1=0           // can be 0 here as we use the missile specific positions from the ammunition file from section [Launchers]