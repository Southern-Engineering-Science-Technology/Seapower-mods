[ ------------------------ Visual/Infared ------------------------- ]

[OLS-27]
Kind=Visual
#Infrared
VIDRangeMultiplier=0.8           // Target VID range multiplied by this for identification check. 0 means it cannot identify
MaxRangeMultiplier=1.1           // Target VID range multiplied by this for detection check. Can not be less then VID multiplier
LookDownMultiplier=0.7             // Final VID/Detection range is multiplied by this in case when target observed on surface background
NightVisionLevel=0.9             // the portion of full daylight added to the visibility level where 1 is roughly a mid-day

[OLS-35]
Kind=Visual
#Infrared
VIDRangeMultiplier=1.1           // Target VID range multiplied by this for identification check. 0 means it cannot identify
MaxRangeMultiplier=1.3           // Target VID range multiplied by this for detection check. Can not be less then VID multiplier
LookDownMultiplier=0.9            // Final VID/Detection range is multiplied by this in case when target observed on surface background
NightVisionLevel=0.9             // the portion of full daylight added to the visibility level where 1 is roughly a mid-day

[oeps-30]
Kind=Visual
Infrared=Ture
VIDRangeMultiplier=1.25             // Target VID range multiplied by this for identification check. 0 means it cannot identify
MaxRangeMultiplier=2           // Target VID range multiplied by this for detection check. Can not be less then VID multiplier
LookDownMultiplier=0.5           // Final VID/Detection range is multiplied by this in case when target observed on surface background
NightVisionLevel=0.9

[------------------------- Radar ---------------------------]

[---Air/Air&Surface---]


[1893]
#Users: plaf_j16a
Kind=Radar
Type=DirectedSearch    // Classes can be "DirectedSearch", "Search", "Targeting"
HasDataLink=True      // true means the sensor data will be used for the entire task force via data link, false local use only
Role=AirAndSurface               // Role can be "Surface", "Air", "AirAndSurface"
Mode=Illuminate        // Can be "Illuminate", "RadioCommand"
TargetChannels=30       // if applicable number of targets it can track simultaneously
WeaponChannels=15       // if applicable number of weapons (missiles) it can guide simultaneously
ViewArcs=-100,100|-100,100               // Field of View of the radar
LookDownMultiplier=0.8     // 多径地形杂波系数，当目标投影到地形/海洋背景上时，应用于最大雷达距离的乘数，默认值为1.0
MinRange=0.3           // min range in km
MaxRange=280           // max range in km
MinAltitude=10         // min altitude in meters, relevant for air, unused for surface
MaxAltitude=30480      // max altitude in meters, relevant for air, unused for surface
RangeResolution=8.5  // defines the minimum range where two targets behind can be recognized as two; in meters
BeamWidth=0.8          // defines the beam width in degrees for BearingResolution >= 2*Dist*sin(BeamWidth/2)
Gain=55             // gain in dB on the sensor
Frequency=X-Band       // frequency band
PeakPower=80.0      //AESA   // output peak power in kW

[1893-A]
#Users: plaf_j16a
Kind=Radar
Type=DirectedSearch    // Classes can be "DirectedSearch", "Search", "Targeting"
HasDataLink=True      // true means the sensor data will be used for the entire task force via data link, false local use only
Role=AirAndSurface               // Role can be "Surface", "Air", "AirAndSurface"
Mode=Illuminate        // Can be "Illuminate", "RadioCommand"
TargetChannels=80       // if applicable number of targets it can track simultaneously
WeaponChannels=15       // if applicable number of weapons (missiles) it can guide simultaneously
ViewArcs=-120,120|-120,120               // Field of View of the radar
LookDownMultiplier=0.9     // 多径地形杂波系数，当目标投影到地形/海洋背景上时，应用于最大雷达距离的乘数，默认值为1.0
MinRange=0.3           // min range in km
MaxRange=320           // max range in km
MinAltitude=10         // min altitude in meters, relevant for air, unused for surface
MaxAltitude=30480      // max altitude in meters, relevant for air, unused for surface
RangeResolution=8.5  // defines the minimum range where two targets behind can be recognized as two; in meters
BeamWidth=0.8          // defines the beam width in degrees for BearingResolution >= 2*Dist*sin(BeamWidth/2)
Gain=65             // gain in dB on the sensor
Frequency=X-Band       // frequency band
PeakPower=80.0      //AESA   // output peak power in kW



[---------------------------------------- ESM ----------------------------------------]
[L004]
Kind=ECM
Frequencies=UHF-Band,L-Band,S-Band,C-Band,X-Band,Ku-Band //radar frequency bands the ECM suite can jam
Type=Universal                      // can be Defensive, Offensive, Universal

[K/RKL700]
#Users: plaf_j16d
Kind=ECM
Frequencies=UHF-Band,L-Band,S-Band,C-Band,X-Band,Ku-Band //radar frequency bands the ECM suite can jam
Type=Universal                      // can be Defensive, Offensive, Universal

PeakPower=35.0                      // output peak power in kW, more power increase effectivness of offensive ECM jammers
MaxRange=250.0                       // max range in km
Gain=20.5                            // Offensive ECM jammer gain in db, more -> better
JamConeViewArcs=50.0                // For offensive and universal systems. Defines the cone along axis between a radar and the ECM system. Targets inside this cone is "covered" by jamming

JamChance=0.5                       // For Defensive and Universal jammers. Base chance to jam the attacking weapon

[KG-800]
#Users: plaf_j16d
Kind=ECM
Frequencies=UHF-Band,L-Band,S-Band,C-Band,X-Band,Ku-Band //radar frequency bands the ECM suite can jam
Type=Universal                      // can be Defensive, Offensive, Universal
PeakPower=19.0                      // output peak power in kW, more power increase effectivness of offensive ECM jammers
MaxRange=200.0                       // max range in km
Gain=7.5                            // Offensive ECM jammer gain in db, more -> better
JamConeViewArcs=35.0                // For offensive and universal systems. Defines the cone along axis between a radar and the ECM system. Targets inside this cone is "covered" by jamming
JamChance=0.7                       // For Defensive and Universal jammers. Base chance to jam the attacking weapon

